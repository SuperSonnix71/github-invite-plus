{
  "name": "github-invite-plus",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "dev:server": "pnpm --filter @gip/server dev",
    "build:server": "pnpm --filter @gip/server build",
    "build:ext": "pnpm --filter @gip/extension-web build"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}